```json
[
  {
    "title": "Elasticsearch Enterprise Search & Log Triage MCP Server for Claude Desktop & Cursor IDE",
    "seo_title": "Elasticsearch MCP Server: Log Triage for Claude Desktop & Cursor",
    "meta_description": "Build and deploy an Elasticsearch Enterprise Search & Log Triage MCP Server for Claude Desktop and Cursor IDE to automate log analysis and observability.",
    "seo_keywords": "Elasticsearch MCP Server, Log Triage AI Agent, Claude Desktop Elasticsearch, Cursor IDE MCP, Enterprise Search AI, FastMCP Elasticsearch, AI Log Analysis",
    "deck": "Unlock autonomous log triage, distributed tracing analysis, and high-performance text search by connecting your Elasticsearch cluster directly to Claude Desktop and Cursor IDE via the Model Context Protocol (MCP).",
    "featured_image": "https://dailyaiworld.com/wp-content/uploads/2026/08/elasticsearch-mcp-server-architecture.jpg",
    "category_id": 5,
    "author_byline": "By [Deepak Bagada](https://x.com/deeepakbagada), CEO at SaaSNext & Principal AI Architect.",
    "content": "By [Deepak Bagada](https://x.com/deeepakbagada), CEO at SaaSNext & Principal AI Architect.\n\n## Introduction: The Observability Data Bottleneck in AI Engineering\n\nIn modern cloud-native architectures, diagnosing a P1 production incident is rarely about lacking data. It is about navigating a flood of logs, traces, and metrics spread across distributed microservices. Engineering teams use Elasticsearch as the backbone of their observability stack due to its unparalleled text search capabilities, robust aggregations, and near real-time ingestion rates. However, when an incident occurs, the context switch between your IDE, your alerting dashboard, and your Kibana interface dramatically slows down the time-to-resolution (TTR).\n\nEnter the **Model Context Protocol (MCP)**. By building an Elasticsearch MCP Server, we can bridge the gap between our observability data and advanced LLM agents inside [Claude Desktop](/latest-ai-news) or the [Cursor IDE](/mcp-directory). Instead of manually crafting Lucene queries, developers can simply ask their AI assistant to \"find the root cause of the 500 errors in the billing service over the last 15 minutes.\"\n\nIn this comprehensive 1,200+ word guide, we will architect, build, and deploy an Elasticsearch Enterprise Search & Log Triage MCP Server using TypeScript and the FastMCP framework. We will explore advanced query translation, secure authentication, index management, and autonomous log summarization for autonomous agentic AI workflows. For more integrations, check out our [MCP Directory](/mcp-directory).\n\n## Section 1: Architectural Blueprint of the Elasticsearch MCP Server\n\nOur Elasticsearch MCP Server is designed to expose a specific subset of Elasticsearch capabilities to the LLM agent securely. We do not want the LLM to have unrestricted delete access or the ability to drop indices. Instead, we want a highly constrained, read-heavy interface optimized for log triage and semantic search.\n\n### 1.1 Core Components\n\n1.  **FastMCP Framework:** We utilize the FastMCP framework for TypeScript to quickly define tools, resources, and prompts with built-in schema validation via Zod.\n2.  **Elasticsearch Node.js Client:** The official `@elastic/elasticsearch` client handles the HTTP communication, connection pooling, and payload compression to our cluster.\n3.  **Authentication Middleware:** API Key or Basic Auth credentials passed via environment variables, ensuring the MCP server runs with the principle of least privilege.\n4.  **Query Translation Engine:** A utility that allows the LLM to request searches using natural language or simplified JSON, which is then translated into robust Elasticsearch Query DSL.\n\n### 1.2 Tool Definitions\n\nWe will expose the following tools to the LLM:\n*   `es_search_logs`: Executes a robust boolean query against time-series log indices, filtering by timestamp, service name, and log level.\n*   `es_get_document_by_id`: Retrieves a specific trace or document by its `_id` for deep inspection.\n*   `es_aggregate_errors`: Runs a terms aggregation on the `error.type` or `message.keyword` fields to find the most frequent anomalies.\n*   `es_cluster_health`: Returns the `_cluster/health` status for infrastructure diagnostics.\n\nFor more complex architectural patterns, see our [AI Workflows](/workflows) section.\n\n## Section 2: Environment Setup and Dependencies\n\nBefore diving into the code, ensure you have Node.js 20+ installed. Create a new directory for your MCP server and initialize the project.\n\n```bash\nmkdir elasticsearch-mcp-server\ncd elasticsearch-mcp-server\nnpm init -y\n```\n\nInstall the necessary dependencies:\n\n```bash\nnpm install @modelcontextprotocol/sdk @elastic/elasticsearch zod dotenv\nnpm install -D typescript @types/node tsx\n```\n\nInitialize TypeScript:\n\n```bash\nnpx tsc --init\n```\n\nUpdate your `tsconfig.json` to ensure strict typing and modern module resolution:\n\n```json\n{\n  \"compilerOptions\": {\n    \"target\": \"ES2022\",\n    \"module\": \"NodeNext\",\n    \"moduleResolution\": \"NodeNext\",\n    \"outDir\": \"./build\",\n    \"rootDir\": \"./src\",\n    \"strict\": true,\n    \"esModuleInterop\": true,\n    \"skipLibCheck\": true,\n    \"forceConsistentCasingInFileNames\": true\n  }\n}\n```\n\nUpdate your `package.json` to include `"type": "module"` and the start scripts:\n\n```json\n{\n  \"name\": \"elasticsearch-mcp-server\",\n  \"version\": \"1.0.0\",\n  \"type\": \"module\",\n  \"scripts\": {\n    \"build\": \"tsc\",\n    \"start\": \"node build/index.js\",\n    \"dev\": \"tsx src/index.ts\"\n  }\n}\n```\n\n## Section 3: Implementing the FastMCP Server logic\n\nCreate a `src` directory and create the entry point `src/index.ts`. We will instantiate the FastMCP server and the Elasticsearch client.\n\n```typescript\nimport { Server } from '@modelcontextprotocol/sdk/server/index.js';\nimport { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';\nimport {\n  CallToolRequestSchema,\n  ListToolsRequestSchema,\n} from '@modelcontextprotocol/sdk/types.js';\nimport { Client } from '@elastic/elasticsearch';\nimport { z } from 'zod';\nimport dotenv from 'dotenv';\n\ndotenv.config();\n\n// Ensure required environment variables are present\nconst ES_NODE = process.env.ES_NODE;\nconst ES_API_KEY = process.env.ES_API_KEY;\n\nif (!ES_NODE || !ES_API_KEY) {\n  console.error(\"Missing ES_NODE or ES_API_KEY environment variables.\");\n  process.exit(1);\n}\n\n// Initialize the Elasticsearch Client\nconst esClient = new Client({\n  node: ES_NODE,\n  auth: {\n    apiKey: ES_API_KEY\n  }\n});\n\n// Initialize MCP Server\nconst server = new Server(\n  {\n    name: 'elasticsearch-mcp-server',\n    version: '1.0.0',\n  },\n  {\n    capabilities: {\n      tools: {},\n    },\n  }\n);\n```\n\n### 3.1 Defining the Schema for `es_search_logs`\n\nThe LLM needs a way to filter logs by time, severity, and text. We use Zod to define a strict schema that the LLM must adhere to.\n\n```typescript\nconst SearchLogsSchema = z.object({\n  index: z.string().describe(\"The Elasticsearch index pattern (e.g., 'logs-app-*')\"),\n  query: z.string().describe(\"The natural language or keyword query to search for in the message field\"),\n  log_level: z.enum([\"ERROR\", \"WARN\", \"INFO\", \"DEBUG\"]).optional().describe(\"Filter by log level severity\"),\n  minutes_ago: z.number().default(15).describe(\"Look back window in minutes\"),\n  size: z.number().max(100).default(10).describe(\"Number of log entries to retrieve (max 100)\")\n});\n```\n\n### 3.2 Implementing the Tool Execution Handler\n\nNow we map the MCP tool calls to actual Elasticsearch Query DSL.\n\n```typescript\nserver.setRequestHandler(ListToolsRequestSchema, async () => {\n  return {\n    tools: [\n      {\n        name: 'es_search_logs',\n        description: 'Search application logs in Elasticsearch with time and severity filters. Highly useful for root cause analysis during incidents.',\n        inputSchema: {\n          type: 'object',\n          properties: {\n            index: { type: 'string', description: 'Index pattern' },\n            query: { type: 'string', description: 'Search term' },\n            log_level: { type: 'string', enum: ['ERROR', 'WARN', 'INFO', 'DEBUG'] },\n            minutes_ago: { type: 'number', description: 'Minutes to look back' },\n            size: { type: 'number', description: 'Max results' }\n          },\n          required: ['index', 'query']\n        }\n      },\n      {\n        name: 'es_cluster_health',\n        description: 'Get the health status of the Elasticsearch cluster (green, yellow, red).',\n        inputSchema: {\n          type: 'object',\n          properties: {},\n          required: []\n        }\n      }\n    ]\n  };\n});\n\nserver.setRequestHandler(CallToolRequestSchema, async (request) => {\n  const { name, arguments: args } = request.params;\n\n  if (name === 'es_cluster_health') {\n    const health = await esClient.cluster.health();\n    return {\n      content: [{ type: 'text', text: JSON.stringify(health, null, 2) }]\n    };\n  }\n\n  if (name === 'es_search_logs') {\n    const parsed = SearchLogsSchema.safeParse(args);\n    if (!parsed.success) {\n      return { content: [{ type: 'text', text: `Invalid arguments: ${parsed.error.message}` }], isError: true };\n    }\n\n    const { index, query, log_level, minutes_ago, size } = parsed.data;\n\n    // Construct the Elasticsearch Boolean Query\n    const must: any[] = [\n      { match: { message: query } }\n    ];\n\n    const filter: any[] = [\n      {\n        range: {\n          '@timestamp': {\n            gte: `now-${minutes_ago}m`,\n            lte: 'now'\n          }\n        }\n      }\n    ];\n\n    if (log_level) {\n      filter.push({ term: { 'log.level': log_level } });\n    }\n\n    try {\n      const result = await esClient.search({\n        index,\n        size,\n        sort: [{ '@timestamp': { order: 'desc' } }],\n        query: {\n          bool: {\n            must,\n            filter\n          }\n        }\n      });\n\n      const hits = result.hits.hits.map(h => h._source);\n      return {\n        content: [{ type: 'text', text: JSON.stringify(hits, null, 2) }]\n      };\n    } catch (error: any) {\n      return {\n        content: [{ type: 'text', text: `Elasticsearch query failed: ${error.message}` }],\n        isError: true\n      };\n    }\n  }\n\n  return {\n    content: [{ type: 'text', text: `Unknown tool: ${name}` }],\n    isError: true\n  };\n});\n\n// Start Transport\nasync function main() {\n  const transport = new StdioServerTransport();\n  await server.connect(transport);\n  console.error(\"Elasticsearch MCP Server running on STDIO\");\n}\n\nmain().catch(console.error);\n```\n\n## Section 4: Connecting to Claude Desktop and Cursor IDE\n\nWith the server built, we need to register it in our AI clients. The Model Context Protocol uses STDIO to communicate securely with local executables. We need to configure the Claude Desktop configuration file.\n\n### 4.1 Claude Desktop Configuration\n\nLocate the `claude_desktop_config.json` file. On MacOS, it is typically at `~/Library/Application Support/Claude/claude_desktop_config.json`.\n\n```json\n{\n  \"mcpServers\": {\n    \"elasticsearch-triage\": {\n      \"command\": \"node\",\n      \"args\": [\"/absolute/path/to/elasticsearch-mcp-server/build/index.js\"],\n      \"env\": {\n        \"ES_NODE\": \"https://your-cluster-id.es.us-central1.gcp.cloud.es.io:9243\",\n        \"ES_API_KEY\": \"your-secure-api-key\"\n      }\n    }\n  }\n}\n```\n\n### 4.2 Cursor IDE Integration\n\nFor developers using the [Cursor IDE](/latest-ai-news), the configuration process is equally straightforward. Navigate to `Cursor Settings > Features > MCP` and add a new MCP server. Select `command` as the transport, set the command to `node`, and pass the absolute path to your compiled `index.js`. Add your environment variables in the provided UI.\n\nOnce connected, you can open the Cursor Composer and say: *\"Check the Elasticsearch logs for `logs-payments-*` over the last 30 minutes. Are there any ERROR level logs mentioning 'Stripe rate limit'? If so, identify the failing lines in our payment controller and suggest a retry mechanism.\"*\n\nThe LLM will autonomously execute the `es_search_logs` tool, parse the JSON response from your Elasticsearch cluster, synthesize the context, and write code to fix the issue directly in your editor.\n\n## Section 5: Advanced Optimization & Production Guardrails\n\nRunning an MCP server connected to a production Elasticsearch cluster requires stringent operational guardrails.\n\n### 5.1 Query Timeout & Circuit Breakers\n\nDo not allow the LLM to run unbounded queries that could spike cluster heap usage. Set a strict `timeout` on the Elasticsearch client (e.g., `requestTimeout: 10000` for 10 seconds). Furthermore, limit the `size` parameter strictly; LLMs have finite context windows, and returning 10,000 log lines will blow out the token limit. We constrained the Zod schema to `.max(100)` for this exact reason.\n\n### 5.2 Field Level Security (FLS) & Role-Based Access Control (RBAC)\n\nNever use the `elastic` superuser credential for the `ES_API_KEY`. Create a dedicated API key with read-only privileges targeting specific index patterns (e.g., `logs-*`). Use Elasticsearch Field Level Security to redact sensitive fields such as `user.password` or `credit_card.pan` so the LLM cannot accidentally ingest PII.\n\n### 5.3 Semantic Search & Vector Embeddings\n\nIf your Elasticsearch cluster uses dense vectors (Elasticsearch 8.x+), you can augment this MCP server with a kNN search tool. Instead of keyword-based log search, the agent can perform semantic search over knowledge base indices or documentation, merging log triage with RAG-based runbook retrieval.\n\n## Conclusion\n\nThe Elasticsearch Enterprise Search & Log Triage MCP Server represents a paradigm shift in how Site Reliability Engineers and AI Agents interact. By surfacing deep observability data directly into the LLM's context window via Claude Desktop or Cursor IDE, we collapse the incident response timeline from hours to minutes. \n\nContinue exploring advanced agentic patterns in our [AI Workflows](/workflows) section, or discover more connectors in the [MCP Directory](/mcp-directory) on Daily AI World.",
    "faqs": [
      {
        "question": "Can I use this MCP server with an on-premise Elasticsearch cluster?",
        "answer": "Yes, as long as the machine running Claude Desktop or Cursor IDE has network access to the Elasticsearch node. You may need to configure custom CA certificates in the client initialization if using self-signed TLS."
      },
      {
        "question": "How does the MCP server handle massive log payloads that exceed LLM context limits?",
        "answer": "The server uses Zod schema validation to hard-cap the `size` parameter (e.g., max 100 documents). Additionally, it filters by specific time windows and severities to ensure the returned JSON payload remains within optimal token limits for Claude 3.5 Sonnet or GPT-4o."
      },
      {
        "question": "Does this work with OpenSearch?",
        "answer": "While Elasticsearch and OpenSearch share DNA, this specific guide uses the official `@elastic/elasticsearch` Node.js client. To adapt it for OpenSearch, you would simply swap the dependency for the `@opensearch-project/opensearch` client and adjust the connection logic."
      }
    ]
  },
  {
    "title": "MongoDB Vector Search & Aggregation Pipeline FastMCP TypeScript Server for AI Agents",
    "seo_title": "MongoDB Vector Search MCP Server TypeScript Guide",
    "meta_description": "Build a MongoDB Vector Search and Aggregation Pipeline FastMCP TypeScript Server for Claude Desktop and Cursor IDE. Learn how to connect AI agents to NoSQL data.",
    "seo_keywords": "MongoDB MCP Server, MongoDB Vector Search, FastMCP TypeScript, Cursor IDE MongoDB, Claude Desktop NoSQL, AI Agents MongoDB, Aggregation Pipeline AI",
    "deck": "Empower your AI agents with semantic retrieval and advanced data manipulation by building a MongoDB Vector Search and Aggregation Pipeline MCP Server using FastMCP and TypeScript.",
    "featured_image": "https://dailyaiworld.com/wp-content/uploads/2026/08/mongodb-vector-mcp-server-guide.jpg",
    "category_id": 5,
    "author_byline": "By [Deepak Bagada](https://x.com/deeepakbagada), CEO at SaaSNext & Principal AI Architect.",
    "content": "By [Deepak Bagada](https://x.com/deeepakbagada), CEO at SaaSNext & Principal AI Architect.\n\n## Introduction: Unifying NoSQL and Vector Context for Agentic AI\n\nModern AI applications require more than just flat text retrieval; they demand complex relational understanding, metadata filtering, and semantic search. MongoDB Atlas has evolved into a premier multi-model database, offering robust NoSQL document storage seamlessly integrated with Atlas Vector Search. However, connecting autonomous AI agents to MongoDB has traditionally required hardcoded middle-tier APIs.\n\nThe **Model Context Protocol (MCP)** changes this paradigm. By building a MongoDB MCP Server, we can expose MongoDB's powerful Aggregation Pipeline and Vector Search capabilities directly to LLMs inside [Claude Desktop](/latest-ai-news) or the [Cursor IDE](/mcp-directory). This allows the AI agent to autonomously craft queries, retrieve semantic context, and analyze complex datasets on the fly.\n\nIn this comprehensive 1,200+ word technical guide, we will construct a high-performance MongoDB Vector Search FastMCP TypeScript Server. We will cover connection pooling, secure credential management, dynamic aggregation generation, and kNN semantic search implementation. To discover more agentic integrations, visit our [MCP Directory](/mcp-directory).\n\n## Section 1: The FastMCP MongoDB Architecture\n\nThe architecture relies on the FastMCP framework for fast, type-safe MCP server creation and the official MongoDB Node.js driver for database interactions. \n\n### 1.1 Core Capabilities Exposed\n\nWe will expose three primary tools to the LLM agent:\n\n1.  `mongo_find_documents`: A standard NoSQL find operation for retrieving documents by exact match, regex, or range queries.\n2.  `mongo_aggregate_pipeline`: Allows the LLM to execute an array of aggregation stages (`$match`, `$group`, `$sort`) for complex data analysis (read-only).\n3.  `mongo_vector_search`: Executes an `$vectorSearch` pipeline stage to find semantically similar documents based on a query embedding.\n\nThis architecture prioritizes safety. We explicitly omit insert, update, or delete operations (`insertOne`, `deleteMany`, etc.) to prevent the AI from mutating production data. \n\nExplore related concepts in our [AI Workflows](/workflows) guides.\n\n## Section 2: Scaffolding the TypeScript Project\n\nInitialize a new Node.js project and install the requisite dependencies.\n\n```bash\nmkdir mongodb-mcp-server\ncd mongodb-mcp-server\nnpm init -y\nnpm install @modelcontextprotocol/sdk mongodb zod dotenv\nnpm install -D typescript @types/node tsx\n```\n\nSet up the `tsconfig.json` for ESNext module compatibility:\n\n```json\n{\n  \"compilerOptions\": {\n    \"target\": \"ES2022\",\n    \"module\": \"NodeNext\",\n    \"moduleResolution\": \"NodeNext\",\n    \"outDir\": \"./dist\",\n    \"rootDir\": \"./src\",\n    \"strict\": true,\n    \"esModuleInterop\": true,\n    \"skipLibCheck\": true\n  }\n}\n```\n\nConfigure `package.json` with module support:\n\n```json\n{\n  \"name\": \"mongodb-mcp-server\",\n  \"version\": \"1.0.0\",\n  \"type\": \"module\",\n  \"scripts\": {\n    \"build\": \"tsc\",\n    \"start\": \"node dist/index.js\",\n    \"dev\": \"tsx src/index.ts\"\n  }\n}\n```\n\n## Section 3: Building the MongoDB MCP Server\n\nCreate `src/index.ts`. We begin by handling the MongoDB connection lifecycle and setting up the FastMCP STDIO server.\n\n```typescript\nimport { Server } from '@modelcontextprotocol/sdk/server/index.js';\nimport { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';\nimport {\n  CallToolRequestSchema,\n  ListToolsRequestSchema,\n} from '@modelcontextprotocol/sdk/types.js';\nimport { MongoClient, ServerApiVersion } from 'mongodb';\nimport { z } from 'zod';\nimport dotenv from 'dotenv';\n\ndotenv.config();\n\nconst MONGO_URI = process.env.MONGO_URI;\nconst DATABASE_NAME = process.env.DATABASE_NAME;\n\nif (!MONGO_URI || !DATABASE_NAME) {\n  console.error(\"Missing MONGO_URI or DATABASE_NAME\");\n  process.exit(1);\n}\n\n// Create a MongoClient with a MongoClientOptions object to set the Stable API version\nconst client = new MongoClient(MONGO_URI, {\n  serverApi: {\n    version: ServerApiVersion.v1,\n    strict: true,\n    deprecationErrors: true,\n  }\n});\n\nconst server = new Server(\n  { name: 'mongodb-mcp-server', version: '1.0.0' },\n  { capabilities: { tools: {} } }\n);\n```\n\n### 3.1 Defining Zod Schemas for Tool Arguments\n\nWe must define robust schemas so the LLM understands exactly how to format its requests, particularly for complex JSON structures like aggregation pipelines.\n\n```typescript\nconst FindSchema = z.object({\n  collection: z.string().describe(\"The MongoDB collection name\"),\n  filter: z.string().describe(\"JSON string representing the query filter (e.g., {\\\"status\\\": \\\"active\\\"})\"),\n  limit: z.number().max(50).default(10)\n});\n\nconst AggregateSchema = z.object({\n  collection: z.string(),\n  pipeline: z.string().describe(\"JSON string of the aggregation pipeline array. E.g., [{\\\"$match\\\": {\\\"age\\\": {\\\"$gt\\\": 18}}} , {\\\"$group\\\": {\\\"_id\\\": \\\"$status\\\", \\\"count\\\": {\\\"$sum\\\": 1}}}]\")\n});\n\nconst VectorSearchSchema = z.object({\n  collection: z.string(),\n  indexName: z.string().describe(\"Name of the Atlas Vector Search index\"),\n  queryVector: z.array(z.number()).describe(\"The dense vector embedding to search for\"),\n  path: z.string().describe(\"The field containing the embeddings\"),\n  numCandidates: z.number().default(100),\n  limit: z.number().max(20).default(5)\n});\n```\n\n### 3.2 Tool Registration and Execution\n\nRegister the tools and handle execution requests. Note that we must parse the incoming JSON strings provided by the LLM into native JavaScript objects for the MongoDB driver.\n\n```typescript\nserver.setRequestHandler(ListToolsRequestSchema, async () => {\n  return {\n    tools: [\n      {\n        name: 'mongo_find_documents',\n        description: 'Find documents in a MongoDB collection based on a JSON filter string.',\n        inputSchema: zodToJsonSchema(FindSchema) // (Assume zodToJsonSchema utility is present or write out manual schema)\n      },\n      {\n        name: 'mongo_aggregate_pipeline',\n        description: 'Run a complex MongoDB aggregation pipeline for data analysis.',\n        inputSchema: zodToJsonSchema(AggregateSchema)\n      },\n      {\n        name: 'mongo_vector_search',\n        description: 'Perform a semantic vector search using MongoDB Atlas.',\n        inputSchema: zodToJsonSchema(VectorSearchSchema)\n      }\n    ]\n  };\n});\n\nserver.setRequestHandler(CallToolRequestSchema, async (request) => {\n  const { name, arguments: args } = request.params;\n  const db = client.db(DATABASE_NAME);\n\n  try {\n    if (name === 'mongo_find_documents') {\n      const parsed = FindSchema.parse(args);\n      const filterObj = JSON.parse(parsed.filter);\n      const results = await db.collection(parsed.collection)\n        .find(filterObj)\n        .limit(parsed.limit)\n        .toArray();\n      return { content: [{ type: 'text', text: JSON.stringify(results, null, 2) }] };\n    }\n\n    if (name === 'mongo_aggregate_pipeline') {\n      const parsed = AggregateSchema.parse(args);\n      const pipelineArray = JSON.parse(parsed.pipeline);\n      \n      // Safety check: ensure pipeline does not contain $out or $merge\n      const hasMutations = pipelineArray.some((stage: any) => \n        stage.hasOwnProperty('$out') || stage.hasOwnProperty('$merge')\n      );\n      if (hasMutations) {\n         throw new Error(\"Aggregation pipeline mutations ($out, $merge) are forbidden.\");\n      }\n\n      const results = await db.collection(parsed.collection)\n        .aggregate(pipelineArray)\n        .toArray();\n      return { content: [{ type: 'text', text: JSON.stringify(results, null, 2) }] };\n    }\n\n    if (name === 'mongo_vector_search') {\n      const parsed = VectorSearchSchema.parse(args);\n      const pipeline = [\n        {\n          $vectorSearch: {\n            index: parsed.indexName,\n            path: parsed.path,\n            queryVector: parsed.queryVector,\n            numCandidates: parsed.numCandidates,\n            limit: parsed.limit\n          }\n        }\n      ];\n      const results = await db.collection(parsed.collection)\n        .aggregate(pipeline)\n        .toArray();\n      return { content: [{ type: 'text', text: JSON.stringify(results, null, 2) }] };\n    }\n\n    return { content: [{ type: 'text', text: `Unknown tool: ${name}` }], isError: true };\n  } catch (error: any) {\n    return { content: [{ type: 'text', text: `MongoDB Error: ${error.message}` }], isError: true };\n  }\n});\n\nasync function main() {\n  await client.connect();\n  console.error(\"Connected successfully to MongoDB Atlas\");\n  const transport = new StdioServerTransport();\n  await server.connect(transport);\n  console.error(\"MongoDB MCP Server running on STDIO\");\n}\n\nmain().catch(console.dir);\n```\n\n*(Note: To strictly conform to FastMCP, you might prefer using the `@modelcontextprotocol/sdk` utility methods or the higher-level `fastmcp` package wrapper, but the raw SDK implementation provides explicit control over the MongoDB connection lifecycle.)*\n\n## Section 4: Configuration and Deployment\n\nTo use this server with your AI coding assistant, you must compile the TypeScript code and register the node binary in your client's configuration.\n\n### 4.1 Claude Desktop Setup\n\nAdd the configuration to `~/Library/Application Support/Claude/claude_desktop_config.json`:\n\n```json\n{\n  \"mcpServers\": {\n    \"mongodb-atlas\": {\n      \"command\": \"node\",\n      \"args\": [\"/absolute/path/to/mongodb-mcp-server/dist/index.js\"],\n      \"env\": {\n        \"MONGO_URI\": \"mongodb+srv://user:pass@cluster0.mongodb.net/?retryWrites=true&w=majority\",\n        \"DATABASE_NAME\": \"production_db\"\n      }\n    }\n  }\n}\n```\n\n### 4.2 Handling Vector Embeddings\n\nWhen using `mongo_vector_search`, the LLM needs to provide the `queryVector`. Since LLMs cannot natively generate dense 1536-dimensional embeddings without calling an external API, you must pair this MongoDB MCP server with an Embedding MCP tool (or allow the LLM to call an OpenAI/Cohere API directly) to convert the user's natural language query into a vector array before passing it to MongoDB.\n\nAlternatively, you can modify the MCP server to accept a string query and handle the embedding generation internally within the server before calling MongoDB.\n\n## Section 5: Security and Production Considerations\n\n1.  **Read-Only User Roles:** The MongoDB user credentials provided in the `MONGO_URI` must be strictly scoped to `read` permissions. Never provide `readWrite` or `dbAdmin` privileges to an autonomous LLM agent.\n2.  **Aggregation Pipeline Guards:** As demonstrated in the code, programmatically block destructive aggregation stages like `$out` and `$merge`.\n3.  **Timeout Limits:** MongoDB queries generated by an LLM might be highly inefficient (e.g., missing indexes). Enforce a `maxTimeMS` on the cursor to prevent long-running queries from blocking the cluster.\n\n## Conclusion\n\nThe MongoDB Vector Search FastMCP TypeScript Server bridges the gap between unstructured LLM reasoning and highly structured, scalable NoSQL datastores. By providing AI agents with read-only aggregation pipelines and semantic retrieval tools, developers can build powerful RAG pipelines, data analysis agents, and autonomous debugging tools directly within their IDE.\n\nExplore more advanced architectures in our [AI Workflows](/workflows) guides, and discover the latest tools in the [MCP Directory](/mcp-directory) at Daily AI World.",
    "faqs": [
      {
        "question": "Can the LLM write and execute complex Aggregation Pipelines on its own?",
        "answer": "Yes. LLMs like Claude 3.5 Sonnet have extensive knowledge of MongoDB Query DSL. By providing the `mongo_aggregate_pipeline` tool, the LLM can autonomously design `$match`, `$group`, and `$unwind` sequences to answer analytical questions."
      },
      {
        "question": "How do I prevent the LLM from executing a table scan that crashes the database?",
        "answer": "Ensure your connection limits `limit` aggressively (e.g., max 50 documents). You should also apply strict timeouts (`maxTimeMS(5000)`) on the MongoDB driver to abort expensive queries."
      },
      {
        "question": "Does Vector Search require MongoDB Atlas?",
        "answer": "Yes, the `$vectorSearch` pipeline stage is a feature exclusive to MongoDB Atlas. If you are using a self-hosted MongoDB Community Edition, you will not be able to use the native vector search capabilities."
      }
    ]
  }
]
```